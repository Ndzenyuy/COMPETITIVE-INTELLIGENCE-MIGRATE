# Phase 1: Core extraction pipeline (scraper, extractor, database)
boto3>=1.34.0
requests==2.31.0
beautifulsoup4==4.12.0
psycopg2-binary==2.9.9
python-dotenv==1.0.0

# Phase 2: Autonomous agent (DuckDuckGo web search)
ddgs==9.14.2

# Phase 3: RAG & vector memory (embeddings, Chroma vector DB)
sentence-transformers==5.5.0
chromadb==1.5.9

# Phase 4: Evaluation suite (semantic scoring)
scikit-learn==1.8.0
numpy==2.4.4

# Phase 5: Dashboard & chat UI
streamlit==1.57.0
pandas==3.0.3
